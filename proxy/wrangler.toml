name = "voucher-chat-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# The Anthropic API key is a SECRET, not a plaintext var. Do not put it here.
# Set it with:  wrangler secret put ANTHROPIC_API_KEY
